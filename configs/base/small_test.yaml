# Small-scale end-to-end test: 100K tokens, K=32, 10 epochs.
# Use this to verify the full pipeline before the 5M-token run.

extraction:
  n_tokens: 100000

model:
  n_clusters: 32

train:
  n_epochs: 10
  diag_every: 50
  reinit_every: 200
  seed: 42
